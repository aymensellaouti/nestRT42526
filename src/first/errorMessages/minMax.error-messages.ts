import { ValidationArguments } from "class-validator";

export const getMinMaxErrorMessage = (isMin = true): (ValidationArguments) => string => {

    // busness logic
    return (validationsArguments:ValidationArguments) => {
        return '';
    }
}